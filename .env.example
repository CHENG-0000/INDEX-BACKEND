# 数据库配置
# 本地开发使用 localhost，生产环境使用云数据库地址
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=daily_records

# 服务器配置
# Render 会自动设置 PORT 环境变量，本地开发使用 3000
PORT=3000
NODE_ENV=development

# 文件上传配置
# 本地开发使用 uploads，Render 上使用 /tmp/uploads
UPLOAD_DIR=uploads
MAX_FILE_SIZE=52428800
