# 🚀 Render 快速部署指南

## 📋 5 步完成部署

### 第 1 步：准备数据库（选择一个）

#### 选项 A：PlanetScale（推荐，完全免费）

1. 访问 https://planetscale.com
2. 注册账号（可用 GitHub 登录）
3. 创建数据库：
   - 点击 "Create database"
   - 名称：`daily_records`
   - 区域：`AWS ap-southeast-1` (新加坡)
4. 初始化表结构：
   - 点击数据库 → "Console"
   - 复制粘贴 `database/init.sql` 中的 SQL
   - 执行
5. 获取连接信息：
   - 点击 "Connect"
   - 选择 "Node.js"
   - 记下：Host, Username, Password

#### 选项 B：Railway（简单，$5/月免费额度）

1. 访问 https://railway.app
2. 注册账号
3. New Project → Database → MySQL
4. 获取连接信息（自动显示）
5. 用 MySQL 客户端连接并执行 `database/init.sql`

---

### 第 2 步：推送代码到 GitHub

```bash
# 1. 进入后端目录
cd d:\Users\agger\Desktop\creat\selfIndex\backend

# 2. 初始化 Git（如果还没有）
git init

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "准备部署到 Render"

# 5. 在 GitHub 创建新仓库
#    访问 https://github.com/new
#    仓库名：daily-records-backend
#    不要初始化 README、.gitignore

# 6. 添加远程仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/daily-records-backend.git

# 7. 推送
git branch -M main
git push -u origin main
```

---

### 第 3 步：在 Render 创建服务

1. 访问 https://render.com
2. 用 GitHub 登录
3. 点击 **"New +"** → **"Web Service"**
4. 点击 **"Connect"** 连接您的 GitHub 仓库
5. 选择 `daily-records-backend`

---

### 第 4 步：配置服务

#### 基本设置：
- **Name**: `daily-records-api`（或任意名称）
- **Region**: `Singapore`（亚洲用户推荐）
- **Branch**: `main`
- **Root Directory**: 留空
- **Environment**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Plan**: `Free`

#### 环境变量（重要！）：

点击 **"Advanced"** → **"Add Environment Variable"**，逐个添加：

```
NODE_ENV = production
PORT = 10000
DB_HOST = <从 PlanetScale 复制>
DB_PORT = 3306
DB_USER = <从 PlanetScale 复制>
DB_PASSWORD = <从 PlanetScale 复制>
DB_NAME = daily_records
UPLOAD_DIR = /tmp/uploads
MAX_FILE_SIZE = 52428800
```

**重要**：DB_HOST、DB_USER、DB_PASSWORD 从第1步的数据库服务复制

---

### 第 5 步：部署并测试

1. 点击 **"Create Web Service"**
2. 等待 3-5 分钟部署完成
3. 部署成功后，复制您的服务 URL：
   ```
   https://daily-records-api-xxxx.onrender.com
   ```

4. 测试 API：
   ```bash
   # 在浏览器打开或使用 curl
   https://daily-records-api-xxxx.onrender.com/api/health
   ```

   应该返回：
   ```json
   {
     "code": 200,
     "message": "服务运行正常",
     "data": {
       "status": "ok",
       "timestamp": "..."
     }
   }
   ```

5. 测试记录接口：
   ```
   https://daily-records-api-xxxx.onrender.com/api/records
   ```

---

## ✅ 部署完成！

您的后端 API 现在已经运行在云端了！

### 🔗 下一步：更新前端配置

修改前端配置文件连接云端后端：

**文件：`frontend/src/utils/request.js`**

找到：
```javascript
baseURL: 'http://localhost:3000/api'
```

改为：
```javascript
baseURL: 'https://daily-records-api-xxxx.onrender.com/api'
```
（替换为您的实际 URL）

---

## ⚙️ 可选：防止服务休眠

Render 免费版 15 分钟无访问会休眠。

### 使用 UptimeRobot（免费）

1. 访问 https://uptimerobot.com
2. 注册账号
3. 添加监控：
   - Type: HTTP(s)
   - URL: `https://daily-records-api-xxxx.onrender.com/api/health`
   - Interval: 5 minutes
4. 保存

这样服务会保持活跃状态。

---

## 🎯 常见 URL 总结

| 服务 | URL |
|------|-----|
| 后端健康检查 | `https://your-app.onrender.com/api/health` |
| 获取记录列表 | `https://your-app.onrender.com/api/records` |
| 创建记录 | `POST https://your-app.onrender.com/api/records` |
| Render Dashboard | https://dashboard.render.com |
| PlanetScale Dashboard | https://app.planetscale.com |

---

## 🐛 问题排查

### 部署失败？

1. 检查 Render 日志：Dashboard → 您的服务 → Logs
2. 常见问题：
   - 环境变量配置错误
   - 数据库连接失败
   - package.json 配置问题

### 数据库连接超时？

1. 确认 PlanetScale 数据库状态正常
2. 检查环境变量拼写是否正确
3. 确认数据库已初始化（执行过 init.sql）

### API 返回 404？

1. 确认 URL 路径正确（包含 `/api`）
2. 检查服务是否完全启动（查看日志）
3. 等待服务唤醒（首次访问可能需要 30-60 秒）

---

## 📚 参考资料

- [Render 文档](https://render.com/docs)
- [PlanetScale 文档](https://planetscale.com/docs)
- [详细部署指南](./部署检查清单.md)

---

需要帮助？欢迎提问！🎉
